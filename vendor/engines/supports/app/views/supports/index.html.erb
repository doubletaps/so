<% content_for :body_content_left do %>
  <ul id="supports">
    <% @supports.each do |support| %>
      <li>
        <%= link_to support.title, support_path(support) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render :partial => "/shared/content_page" %>
